[project]
name = "claude-code-mercury"
version = "0.1.0"
description = "Claude Code to Mercury 2 bridge with Agentic API Standard enrichment"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "live: tests that require a live MERCURY_API_KEY and hit the real Mercury API",
]
